# Processed outputs (regenerated by the build; catalog.json is emitted straight
# into the frontend's static tree when the frontend build invokes this package).
/dist/

# Raw asset drop consumed by scripts/import-card-assets.mjs (not tracked).
/ref/

# Intermediate batches from scripts/import-card-assets.mjs
/cards/.partials/
