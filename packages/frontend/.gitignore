# Assets (not tracked in git)
/Textures/

# Served game data — the source of truth lives in the `data` package. The card
# catalog is generated by build-card-catalog; the rest is copied in by
# sync-served-data. Both run before `dev`/`build` (see the gen:data script).
/static/cards/catalog.json
/static/card-effects/
/static/decks/
/static/dice/
