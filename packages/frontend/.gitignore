# Assets (not tracked in git)
/Textures/

# The only served card dataset, generated at build time by the card-data
# package (scripts/build-card-catalog.mjs) from its cardinfo.json plus this
# package's admin-authored assignments.json + positions.json.
/static/cards/catalog.json
